struct Vec3{
    x: i32,
    y: i32,
    z: i32,
}

length_squared(vec: Vec3): i32 {
    const square: i32 = vec.x * vec.x + vec.y * vec.y + vec.z * vec.z;
    return square;
}

main() {
    var vec: Vec3;

    vec.x = 5;
    vec.y = 7;
    vec.z = 12;

    printi(length_squared(vec));
}
