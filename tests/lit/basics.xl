// RUN: %run --split-input-file %s | %filecheck --match-full-lines %s

main() {
    const a: int = 5;
    // CHECK: 5
    printi(a);
}

//---------------------------------------

main() {
    // CHECK: Hello World!
    prints("Hello World!");
    const a: string = "teststring";
    // CHECK: teststring
    prints(a);
}

//---------------------------------------

main() {
    // CHECK: 8
    printi(5 + 3);
}

//---------------------------------------

main() {
    // CHECK: 15
    printi(5 * 3);
}

//---------------------------------------

main() {
    // CHECK: true
    printb(5 != 3);
}

//---------------------------------------

a() {
    return;
}
b(): int {
    return 9;
}
main() {
    a();
    // CHECK: 9
    printi(b());
}

//---------------------------------------

test(): int {
    return 5;
}
main() {
    const a: int = 1 + 2;
    // CHECK: 3
    printi(a);

    var b: int = 5;
    const c: int = a + b;
    // CHECK: 8
    printi(c);

    b = 5;
    // CHECK: 5
    printi(b);

    const d: int = a * b;
    // CHECK: 15
    printi(d);

    const e: int = a * b + c;
    // CHECK: 23
    printi(e);

    const f: int = a + b * c;
    // CHECK: 43
    printi(f);

    const g: int = test();
    // CHECK: 5
    printi(g);

    const h: int = b + 1;
    // CHECK: 6
    printi(h);

    const i: int = 1 + b;
    // CHECK: 6
    printi(i);
}

//---------------------------------------

main() {
    var i: int = 0;
    loop {
        i = i + 1;
        if (i == 5) {
            break;
        }
    }
    // CHECK: 5
    printi(i);
}

//---------------------------------------

main() {
    // CHECK: 7
    printi(1 + 2 * 3);
    // CHECK: 9
    printi((1 + 2) * 3);
    // CHECK: 7
    printi(1 + (2 * 3));
}

//---------------------------------------

main() {
    const a: float = 3.5;
    const b: float = 1.2;

    // CHECK: 3.5
    printf(a);
    // CHECK: 4.7
    printf(a + b);
    // CHECK: 4.2
    printf(a * b);
}
